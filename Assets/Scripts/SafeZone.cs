using UnityEngine;

public class SafeZone : MonoBehaviour
{
    // This script only serves to tag a platform segment as safe for Precision Mode.
    // All collision logic is handled in BallController.cs.
}